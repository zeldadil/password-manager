QA-VERDICT: pass — the marker path, and the deferral marker with it, now records a verdict only when a `qa`-profile comment wrote it; nothing is dropped silently (`A7_VERDICT_AUTHOR_IGNORED` for a discounted marker, `A8_VERDICT_SELF_DECLARED` for a non-qa run's metadata — still accepted per §3 / AC 1c). Gate sha256 `85dbc127…` (was `28b0b771…`).

**Defect as reported, reproduced first.** Through the real hook path on a board **copy** with the revision installed in all 7 profiles: card `t_7918f010` + one comment `QA-VERDICT: pass — evidence: tests/evidence/t_5fe41426/README.md` (committed @`543c396`) → `check` exit **0**, hook stdout `{}`, **exit 0 (completion ALLOWED)**. The same comment text authored by `qa` behaves identically; stripping it returns both to red. Authorship was the only variable — on the fixed gate the architect row is `check` exit 1 (`R1`+`R4`) / hook exit 2 (block, reason names `R1`), the qa row still allows, the stripped control is still red.

**Acceptance criteria**
1. Selftest 66 → **81 cases, 81/81** on the fixed gate; the same suite against the pre-fix revision (`28b0b771`, extracted byte-exact from `4c0d3ea`) is **73/81** with exactly the **8** new cases red — e.g. `(a) the marker comment authored by architect does NOT satisfy R1 — exit=0 rules=[]` and the hook fire returning `{}`. (a) non-qa marker ⇒ no verdict, `R1` fires, `A7` reported; (b) the same text authored by `qa` ⇒ 0 violations (non-vacuity); (c) loose path + run metadata keep their behaviour (anti-degradation controls, plus a non-qa `blocked` marker no longer creates `R3` and a non-qa `deferred` marker neither satisfies `R1` nor is judged by `R8`).
2. Fixture reproduction above; the control is still red on both revisions.
3. `QA_SIGN_OFF_GATE.md` §3 now states the author rule for **every** verdict source — marker, loose comment, **run metadata** (the one deliberate author-independent source, with the reason and the `A8` visibility), and the deferral marker; §4 gains `R1` phrasing + the `A7`/`A8` rows; §5.1/§5.4 state the requirement; §10 item 8 records the run-metadata decision; §11 changelog.
4. Re-installed in all 7 profiles: installed sha256 == repo copy `85dbc127…`. `verify-signoff-gate.sh --all --live` → **84 ok · 4 warn · 3 FAIL**, the 3 being the documented two-hooks-per-profile `hermes hooks doctor` mtime drift (same pattern as the parent card). Per-profile live fire through the dispatcher path (`hermes hooks test pre_tool_call --for-tool kanban_complete`): **7/7 blocked** the non-QA marker with `R1_QA_VERDICT_MISSING`, **7/7 allowed** the `qa`-authored one.

**Live-board delta, measured on pristine copies (the live board was only ever read).** 3 cards newly failing — `t_e348e0b7`, `t_28951254`, `t_2162d273`: they never had a QA verdict, their only verdict record was an `architect` marker; 1 cleared — `t_28f60dc1` (its `R5` came from the non-QA marker claiming a missing path; now adjudicated through the linked-child deferral to `t_7dd3b960`); 2 changed — `t_527d4720` (`R2` → `R1`+`R2`) and `t_80fc0326` (`R2`×3 → +`R8`: with the architect deferral markers discounted, the linked-child fallback names `t_c3cb6842`, a `qa` card that is `done` without a verdict — a real gap the defect was hiding); 43/49 `done` cards unchanged; enforced `done` cards 6 FAIL/16 pass → **8 FAIL/14 pass**. The surfaced gaps are routed to `t_d20787de` (retro-verification), not silently accepted.

**Scope notes.** `BE-915` (stale-deferral fixture) is now `qa`-authored: under the author rule a non-QA marker is not a live deferral, so the *ordering* case that fixture exists for would have been vacuous — it still passes on both revisions, so no case was weakened. Run metadata stays author-independent (AC 1c) and is now audit-visible (`A8`), with the follow-up decision recorded as §10 item 8. Residual, unchanged: the board is writable by the profiles themselves, so this closes the *quiet* path (no forgery needed, only the wrong author), not a hostile board write.

**PR #31** — stacked on PR #29's head `qa/t_99e408c5-r5-quoted-path` (base = that branch), so it must land **after** PR #29; it adds only this card's three commits.

Evidence: tests/evidence/t_338f47fd/README.md, tests/evidence/t_338f47fd/reproduce-output.txt, tests/evidence/t_338f47fd/selftest-GREEN-fixed-gate.txt, tests/evidence/t_338f47fd/selftest-RED-prefix-gate.txt, tests/evidence/t_338f47fd/audit-both-revisions.txt, tests/evidence/t_338f47fd/ab-live-audit.txt, tests/evidence/t_338f47fd/non-qa-marker-inventory.txt, tests/evidence/t_338f47fd/hook-live-fire-author-rule.txt, tests/evidence/t_338f47fd/installed-hashes.txt, tests/evidence/t_338f47fd/verify-all-live.txt, tests/evidence/t_338f47fd/gate-fix.diff — committed on `qa/t_338f47fd-marker-author` and mirrored into this card's workspace.
