# FE-001h configuration inspection at c91842565324eae89a92d1ff53f7abc536bc790c (origin/feature/t_f49d448c)

## apps/web/eslint.config.mjs
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import prettier from 'eslint-config-prettier'

export default tseslint.config(
  { ignores: ['dist', 'node_modules', 'coverage'] },
  {
    extends: [js.configs.recommended, ...tseslint.configs.recommended],
    files: ['**/*.{ts,tsx}'],
    languageOptions: {
      ecmaVersion: 2022,
      globals: globals.browser,
    },
    plugins: {
      'react-hooks': reactHooks,
      'react-refresh': reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
    },
  },
  prettier,
)

## apps/web/.prettierrc.json
{
  "semi": false,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100
}

## strict flags
tsconfig.base.json:8:    "strict": true,
apps/web/tsconfig.app.json:16:    "strict": true,
apps/web/tsconfig.node.json:13:    "strict": true,

## root package.json (scripts + packageManager)
{
  "name": "password-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted password manager — encrypted vault, Web UI, Firefox extension",
  "scripts": {
    "dev": "pnpm -r --parallel --filter '**' run dev",
    "build": "pnpm -r --parallel --filter '**' run build",
    "lint": "pnpm -r --parallel --filter '**' run lint",
    "typecheck": "pnpm -r --parallel --filter '**' run typecheck",
    "test": "pnpm -r --parallel --filter '**' run test",
    "test:integration": "pnpm -r --parallel --filter '**' run test:integration",
    "format": "pnpm -r --parallel --filter '**' run format",
    "format:check": "pnpm -r --parallel --filter '**' run format:check",
    "clean": "pnpm -r --parallel --filter '**' run clean 2>/dev/null || true"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "workspaces": [
    "apps/web",
    "apps/browser-firefox",
    "apps/services/api",

## apps/web/package.json
{
  "name": "@password-manager/web",
  "version": "0.1.0",
  "private": true,
  "description": "React 18 + TypeScript + Vite — Web UI",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b",
    "lint": "eslint . && prettier --check .",
    "lint:fix": "eslint . --fix && prettier --write .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --passWithNoTests 'src/**/*.integration.test.{ts,tsx}'",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@testing-library/dom": "^10.0.0",
    "@testing-library/react": "^16.0.0",
    "@types/node": "^24.13.3",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^6.1.1",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "10.1.1",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.0",
    "globals": "^15.0.0",
    "jsdom": "^26.0.0",
    "prettier": "^3.0.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.0.0",
    "vite": "^8.3.0",
    "vitest": "^3.0.0"
  }
}
