QA-VERDICT: pass — retro-verified 2026-09-18 against architecture/adr/ADR-005-extension-bridge-protocol.md@2d8e91f94144470fe3aac051f86d86b9346d47c1 (retro-verification: re-checked after the fact, not a verdict recorded at completion time). Evidence: architecture/adr/ADR-005-extension-bridge-protocol.md

**Retro-verification of ADR-005 (`t_08b02da9`, ARC-001d, completed 2026-09-16, pre-epoch).** Recorded by `t_4242bee8` (QA-001i-fu1). Artifact re-read from `origin/master` (tip `94fb9de`); blob sha `2d8e91f94144470fe3aac051f86d86b9346d47c1` confirmed both by `git ls-tree origin/master architecture/adr/ADR-005-extension-bridge-protocol.md` and by `git hash-object` on the extracted file (sha256 `a5299a1800c3fc3d82da2ef8c2d1b5ba7f5d7696639b28a9a65e656e3944e33d`).

Acceptance criteria, re-checked one by one:

| # | criterion | result |
|---|---|---|
| 1 | message types, origin validation, permission model, autofill flow documented | **met — all four.** Message types §2 (§2.1 table of 6 types with direction, purpose and payload shape: `EXTENSION_READY`, `LOCK_STATE_CHANGED`, `AUTOFILL_REQUEST`, `AUTOFILL_RESPONSE`, `VAULT_SEARCH`, `VAULT_SEARCH_RESPONSE`; §2.2 design notes; §2.3 `BridgeMessage` envelope + silent drop of unknown types) · origin validation §3 (§3.1 extension side — `sender.origin` must exactly match the configured web-app origin, configured at build time, never guessed from `location.origin`; §3.2 web-app side — only `moz-extension://` targets, response `source` verified; §3.3 `SameSite=Strict` cookie fallback for lock state only; §3.4 what it prevents) · permission model §4 (§4.1 extension permissions, §4.2 what the bridge does **not** grant, §4.3 permission model vs. autofill flow) · autofill flow §5 (§5.1 flow steps, §5.2 optional bridge-mediated path, §5.3 what autofill must not do) |
| 2 | references ADR-001, explicitly flags any Passbolt-inspired vs. original decision | **met** — header `**References:** ADR-001 (functional patterns), ADR-002 (overall architecture, Section 6), SEC-001 (threat model, Decision 9), PROJECT_BRIEF.md`; §8 with §8.1 (9 decisions, each with *Passbolt source* + *Our adaptation*), §8.2 (original decisions, each with why), §8.3 (rejected, cross-referencing ADR-001), citing the IP-boundary rule from PROJECT_BRIEF §4 |

Verdict **`pass`**: both stated criteria hold against the committed blob, and the four required topics each have a dedicated, substantive section rather than a stub.

**Notes, not conditions.** (a) Neither criterion requires a signature in the document, so the `| QA | (pending) | | |` row in §11 and the `**Status:** Proposed` header do not affect this card — the unsigned-ADR-set observation is recorded on `t_9840ccdd` and folded into `t_816a87b5`. (b) §9.2 states that AR-6 as written does **not** cover `packages/shared/` bridge-message changes and asks for a separate review gate (flagged for DOC-001e), and §10 carries open questions downstream. That is an explicit, routed limitation of the ADR, not an unmet criterion of the card that produced it; it is the same question the `R7` re-classification routes to the architect, not something this retro-verification may decide.
