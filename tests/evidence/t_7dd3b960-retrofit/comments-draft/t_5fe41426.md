QA-VERDICT: pass-with-conditions — record repaired 2026-09-18 by QA-001i-fu1 (`t_4242bee8`). QA-001b's own record carried **evidence but no verdict**: the verdict was deferred to its child `t_a2cf1744`, whose landed verdict is `pass-with-conditions`. This comment records that landed outcome and the missing pointer on this card; it is **not** a fresh verification of the CI pipeline, and it changes no token recorded elsewhere. Evidence: tests/evidence/t_5fe41426/README.md, .github/workflows/ci.yml, https://github.com/zeldadil/password-manager/pull/7

**What was broken (measured).** `node scripts/qa/signoff-gate.mjs audit --db ~/.hermes/kanban.db --repo <full clone> --strict-history` reported:

```
FAIL t_5fe41426  R4_EVIDENCE_MISSING: QA deferral target t_a2cf1744 carries a verdict but no evidence
                 A1_HISTORY_UNGATED: --strict-history: pre-epoch card is not compliant
```

* `R4` is cleared by the repair of `t_a2cf1744` (same date), which now names committed evidence instead of keeping its pointer only in run metadata.
* `A1` is **not** cleared by that edit alone — and this is a measured correction to a claim in the retrofit proposal, which said the deferral target's repair was "the same edit" that fixes this card. Under `--strict-history`, `A1_HISTORY_UNGATED` fires when the card has no valid verdict of its own **or** no evidence pointer. A deferral satisfies `R1` but leaves the card's own valid-verdict list empty, so the card keeps failing the retrofit view. The deferral's own rule — the deferred verdict **and its evidence must live on the target** — is exactly why the landed verdict also has to be visible here.

**Committed pointers added** — each resolvable with `git rev-list --max-count=1 --all -- <path>`:

| pointer | first commit on a ref |
|---|---|
| tests/evidence/t_5fe41426/README.md | 543c396 (`feature/t_5fe41426`) |
| .github/workflows/ci.yml | bf9da1ea (and present on `master` today, `name: CI`, 8 jobs) |
| https://github.com/zeldadil/password-manager/pull/7 | n/a (live PR URL) |

**Conditions, carried over from the deferral target (not re-derived here).** WebKit unverified locally (94 OS packages, no root; CI `--with-deps` resolves it) and the `e2e` job cannot produce a live run until `t_ee24fd37` commits the pnpm manifest + lockfile, because `install-lockfile` blocks every downstream job.

**QA-001b's own recorded finding stays open and is not softened by this repair:** `pnpm -r --parallel --filter '**' run <script>` **returns exit 0 while running nothing** when no workspace defines the script (reproduced: `pnpm test:e2e` → "None of the selected packages has a test:e2e script", exit 0), so a green CI run can be a vacuous pass. The recorded mitigation — every workspace ships its own `vitest run`, which fails on zero tests, with QA-001e adding the Tier A coverage thresholds — is in `tests/evidence/t_5fe41426/README.md` §"Finding — vacuous-pass". It is a documented limitation of the deliverable, which is why this card's token is not `pass`.

**Follow-up:** `t_ee24fd37` (bootstrap blocker; owns the condition that also gates this card's live `e2e` run).
