QA-VERDICT: pass-with-conditions — retro-verified 2026-09-18 against architecture/adr/SEC-001-threat-model.md@3e1bc982f6ae1d36ba091116ff39ceef9ce89780 (retro-verification: re-checked after the fact, not a verdict recorded at completion time). Evidence: architecture/adr/SEC-001-threat-model.md

**Retro-verification of SEC-001 (`t_9840ccdd`, completed 2026-09-16, pre-epoch).** Recorded by `t_4242bee8` (QA-001i-fu1). Artifact re-read from `origin/master` (tip `94fb9de`); blob sha `3e1bc982f6ae1d36ba091116ff39ceef9ce89780` confirmed two ways — `git ls-tree origin/master architecture/adr/SEC-001-threat-model.md` and `git hash-object` on the extracted file (sha256 `4a249465c70d13fe30a303ac89c8cfe1943e05dc93c6736644ef19631da64f06`).

Acceptance criteria, re-checked one by one:

| # | criterion | result |
|---|---|---|
| 1 | threat model covers all 7 minimum vectors | **met** — Part 1, `### Vector 1` … `### Vector 7`: local file/backup access · partial API compromise · hostile extension/malicious page · leakage via logs/errors/URLs/telemetry · stolen device/unlocked session · forgotten master password · corrupted backup/failed migration. Each records scenario, assets, attack surface, controls and residual risk |
| 2 | all 9 mandatory crypto decisions documented **with rationale** | **met** — Part 2, `### Decision 1` … `### Decision 9`, each with an explicit `**Rationale:**` block: D1 KDF (Argon2id, 64 MB / 3 iterations / 4 lanes / 32-byte output, per-user salt), D2 AEAD (AES-256-GCM, 12-byte nonce, 16-byte tag, alternatives considered), D3 key hierarchy + storage schema, D4 nonce/IV (CSPRNG, never reused, never `Math.random`), D5 integrity (tag verified on every decrypt, no plaintext on failure), D6 lock/unlock lifecycle (derive → hold in process memory → clear), D7 recovery kit, D8 encrypted backup, D9 browser bridge |
| 3 | absolute rules explicitly acknowledged | **met** — Part 3, `### AR-1` … `### AR-6`: no home-grown crypto · no real secrets anywhere (code/tests/logs/errors/URLs/env/config/DB/telemetry) · positive + negative tests per crypto change · synthetic fixtures only · migration and rollback strategy · code review for crypto changes |
| 4 | signed off by **Architect + QA**, **recorded in the document** | **NOT MET** — §5 Sign-off row 2 is `\| QA \| (pending) \| \| \|` and the header still reads `**Status:** Signed (Architect) — Pending QA sign-off`. `git grep` over `$(git rev-list --all) -- architecture/adr/` finds the same `(pending)` row on **every ref**: no QA signature was ever recorded in the artifact. §5's own prose says "**QA sign-off is still required.** … the QA profile (`qa`) must also sign before this gate is fully closed and downstream tasks may start" |
| 5 | gate blocks the downstream secret-storage tasks (BE-002, BE-003, BR-002, BR-003, FE-002, FE-003) via explicit `--parent` links | **met (transitively)** — measured over `task_links`: the transitive closure from `t_9840ccdd` reaches **112** descendants and covers **54/54** of the named families (BE-002 8/8 · BE-003 11/11 · BR-002 8/8 · BR-003 7/7 · FE-002 8/8 · FE-003 12/12). Direct children: `t_16f8ad84` (BE-002a), `t_fe3b76ea` (BE-003a), `t_83dc1b35` (BR-002a), `t_3ca45da2` (ADR-002), `t_93cf5611` (DOC-001d). Part 4's note that FE-002/FE-003/BR-003 hang off BE-002h/BE-003k/BR-002h rather than off SEC-001 directly is **accurate**, and those chains exist on the board (`t_8d52bcc9` → `t_3b58b7ef`; `t_e4341d18` → `t_a9d5cb1b`; `t_7b33595a` + `t_e4341d18` → `t_4b263ba0`) |

**Why this is not `pass`.** Criterion 4 is a stated acceptance criterion of the card, not a courtesy — SEC-001 *is* the security gate, and its own Purpose makes Architect **+ QA** sign-off the precondition for writing downstream secret-storage code. The card was completed with that criterion unmet. This verdict does **not** sign the document, and must not be read as one: a retro-verification of the card's acceptance criteria is not a QA sign-off on the threat model.

**Why it is not `fail`.** Four of five criteria are met and verified against the committed blob; the missing item is the §5 signature row, not a content defect — nobody has to redo the threat model, only to close the record.

**Condition (owner `architect`), tracked as `t_816a87b5`.** Either record the QA half of the Sign-off in §5 and flip `**Status:**` off "Pending QA sign-off", or record an explicit decision that the gate is closed by the Architect signature alone — plus the matching changelog row. If the first option is chosen, QA's signature needs a QA card that reviews the document in full; this retrofit verdict is deliberately not a substitute for it.

**Residual risk today is contained.** The gate held: `t_16f8ad84` (BE-002a), `t_fe3b76ea` (BE-003a) and `t_83dc1b35` (BR-002a) are still `todo`/`triage` — no crypto or secret-storage code has been written, so the unmet signature has not been overtaken by implementation.

**Observation, reported not fixed (same class, outside the other cards' criteria).** The `| QA | (pending) | | |` §11 row is also present in ADR-002, ADR-003 and ADR-005, and all four documents are `**Status:** Proposed`. It does not change the verdicts of `t_3ca45da2` / `t_08b02da9` (neither card's acceptance criteria require a signature — both were retro-verified `pass` on their content), but the ADR set is formally unsigned; folded into `t_816a87b5`.
