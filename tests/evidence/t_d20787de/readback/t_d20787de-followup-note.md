Follow-up recorded (this comment is not a verdict record and does not change the operative verdict above).

- `t_df8e644a` — P1, gate lane, gated by this card: the loose-path verdict false positive (the residual half of `t_c3cb6842`), with the reproduction from `tests/evidence/t_d20787de/loose-path-repro.txt`, four acceptance criteria and the anti-degradation controls it must keep.
- The other three findings stay where they were recorded: the PR #31 `§10` item-1 revert on `t_527d4720`, the stale `.gitleaksignore` / branch history on `t_28951254`, and the missing FE-001d theme writer on `t_2162d273`.
- Nothing else is pending on this card; the live board gained only the seven comments of this sweep (ids 161–167).
