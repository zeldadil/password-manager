QA-VERDICT: pass — RETRO-VERIFICATION recorded by the `qa` profile (2026-09-19, from card `t_d20787de`).

This verdict was **not** recorded at completion time. `t_338f47fd` §3 showed that this card's only verdict record was an `architect`-authored marker — discounted by the §3 author rule — so the card was passing on a non-QA record. Re-measured today against the board's own attachment and the repo's refs.

## Acceptance criterion, re-measured today

**"A written decision naming the owning card (or an explicit out-of-scope note) for the theme-toggle writing mechanism and the sidebar-collapse wiring" — HOLDS.** The attachment `decision.md` (attachment id 48, uploaded 2026-09-18 21:18) names, explicitly and unambiguously:

- theme toggle: owning card **FE-001d** (`t_e2b31691`, "Theme System"), mechanism "a `useEffect` in the theme-system component (or `AppShell`) that reads `useThemeStore.theme` and writes `document.documentElement.dataset.theme` (and updates `meta[name=color-scheme]`); the control button itself also belongs to FE-001d";
- sidebar collapse: owning card **FE-003b** (`t_aca6ed13`, "Sidebar"), "wire `Sidebar` and `AppShell` to read `useUiStore.sidebarOpen` and call `toggleSidebar` on user action";
- scope answer: "Not out of scope. No new card needed; extend FE-001d and FE-003b AC with the wiring items", and FE-001k explicitly not blocked.

Both named owner cards exist on the board (FE-001d `done`, FE-003b `todo`). The criterion is satisfied.

## Findings (not conditions on this card)

1. **Follow-through not yet visible in the repo.** `git grep` across all 40 remote branch tips finds no writer of `documentElement.dataset.theme` / `data-theme` in application code (only `theme.css` plus store/test files). FE-001d is already `done` while FE-003b is still `todo`, so the decided mechanism is not yet implemented anywhere — worth a look by the FE lane before FE-003b closes.
2. Cosmetic: the attachment's first line is mojibake (`#<replacement>cope decision…` with literal `\n` escapes); the body is legible and unambiguous.

Evidence: decision.md (attachment id 48 of this card)

Re-measurement transcripts: tests/evidence/t_d20787de (retro-verification bundle of card t_d20787de — cited, not this card's own evidence).
