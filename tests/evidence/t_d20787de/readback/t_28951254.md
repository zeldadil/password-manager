QA-VERDICT: pass-with-conditions — RETRO-VERIFICATION recorded by the `qa` profile (2026-09-19, from card `t_d20787de`).

This verdict was **not** recorded at completion time. `t_338f47fd` §3 showed that this card's only verdict record was an `architect`-authored marker — discounted by the §3 author rule — so the card was passing on a non-QA record. Re-measured today on the host and in the repo; no claim is taken from the card's own summary.

## Acceptance criteria, re-measured today

1. **All 7 profiles' `.env` carry the live rotated value, no dead value anywhere — HOLDS in substance, with the AC's literal check superseded.** Measured: each of `architect, backend, browser, docs, frontend, product, qa` has exactly one active `TELEGRAM_BOT_TOKEN` line; all 7 values are identical (`sha256` prefix `c9180856…`, never the value) and that value is **live** (`getMe` → HTTP 200, `@AASLlmHermesBot`, id `8615677595`); no file carries the revoked value (`62fe6fe5…`) or the value this card propagated (`df5ccd96…`). The AC's "compare against the sha256 read from the board comment" is no longer reproducible **by design**: the board comment was scrubbed (`t_b51a1ff3`) and the value was rotated again after this card (`t_9b3bdba0`).
2. **Duplicates removed, `config.yaml` checked — HOLDS.** Exactly one uncommented `TELEGRAM_BOT_TOKEN` per file; all 7 `config.yaml` plus `~/.hermes/config.yaml` contain **zero** token-shaped values.
3. **Channel works again — HOLDS.** `hermes send --to telegram:956145756` → exit 0 from `qa` and from `backend` (real sends today), and `getMe` for the configured value → 200. Precision note: `hermes send` resolves the bot credential from `~/.hermes/.env`, whose value is the same live value all 7 profiles hold, so this verifies the live credential + channel, and the per-profile propagation is verified by the fingerprint check in AC1.
4. **Evidence committed on a branch, not master — HOLDS.** `tests/evidence/t_28951254/README.md` exists on `fix/telegram-token-rotation-t_28951254` (tip `43f0d07`, 4972 bytes, redaction statement present, no token and no token hash published).

## Conditions

- **AC5(b) cannot be substantiated and is therefore not claimed.** The recorded CI run `35265374451` does **not** exist (CI API → 404) and `gh run list --branch fix/telegram-token-rotation-t_28951254` returns **no runs at all** — so "CI secret-scan green on the branch" was never observable. AC5(a) does hold today: on the branch tip, `trufflehog filesystem tests/evidence/t_28951254 --results=verified,unknown --fail` → exit 0 and `gitleaks dir` → "no leaks found"; the branch tip tree carries no token-shaped string.
- **Branch hygiene, follow-up for the cleanup lane (not a live risk).** That branch's `.gitleaksignore` suppression for the historical `a503e4d` no longer matches anything (that commit was rewritten away, `git cat-file -e` → absent), and the branch **history** still trips gitleaks (4 findings, redacted, none at the tip). The value in all of them is revoked. Prefer deleting or re-cutting this stale branch rather than carrying a stale allowlist.

Evidence: tests/evidence/t_28951254/README.md

Re-measurement transcripts: tests/evidence/t_d20787de (retro-verification bundle of card t_d20787de — cited, not this card's own evidence).
