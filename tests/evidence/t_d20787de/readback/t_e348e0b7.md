QA-VERDICT: pass — RETRO-VERIFICATION recorded by the `qa` profile (2026-09-19, from card `t_d20787de`).

This verdict was **not** recorded at completion time. `t_338f47fd` §3 showed that this card's only verdict record was an `architect`-authored marker — discounted by the §3 author rule — so the card was passing on a non-QA record. Everything below was re-measured today against the committed artifacts and the CI API; no claim is taken from the card's own summary.

## Acceptance criteria, re-measured today

1. **master's `secret-scan` is full-history and verification-based — HOLDS.** `origin/master:.github/workflows/ci.yml` (blob `06a0ee405aec05bb84c7a5e76cc6b63171067d`) `secret-scan` job runs `gitleaks/gitleaks-action@v2` **plus** truffleHog `3.97.5` with `./trufflehog git file://. --no-update --results=verified,unknown --fail` and `actions/checkout@v4` at `fetch-depth: 0`; the `sast` job (Semgrep) is present too.
2. **Negative-control red case — HOLDS on the record, re-read from the CI API today.** Run `35259047666` (PR #21) → `secret-scan: failure`; run `35261467319` (push to master `0e11a80`) → `secret-scan: failure`. Stated plainly rather than glossed: re-running the exact wired command on a full clone **today** returns exit 0 with `verified_secrets: 0, unverified_secrets: 0`, because the credential is revoked (`getMe` → 401) and a dead value is no longer a *verified* finding — the verified-red case is evidenced by those two CI runs, not by today's run.
3. **Wiring on master, not only in an unmerged PR — HOLDS.** `0e11a80` is in `origin/master`'s history and master's tree carries the file above.
4. **No undocumented allowlist/exclusion — HOLDS.** `.gitleaksignore`, `.trufflehogignore` and `.gitleaks.toml` are all absent from master's tree.
5. **Evidence committed under `tests/evidence/<this-task-id>/` — HOLDS.** The README documents the red case (`exit 183`, 3 verified `TelegramBotToken` findings, the CI URLs) and explicitly does **not** claim the green case; the raw reproduction output is committed beside it.

## Residual — measured, accepted, no action

`tests/evidence/t_e348e0b7/trufflehog-origin-master-output.txt` on master's tip tree still re-publishes the leaked string 3× (10 further branch tips carry the same file). Measured today: that value is the revoked one (`getMe` → 401), **no live credential appears on any of the 40 remote branch tips**, and the human decision on `t_80fc0326` (comment 80) classifies dead-credential residue as hygiene-only and declines further purge rounds. Recorded here as an accepted residual, not as a new condition.

Evidence: tests/evidence/t_e348e0b7/README.md, tests/evidence/t_e348e0b7/trufflehog-origin-master-output.txt, .github/workflows/ci.yml, https://github.com/zeldadil/password-manager/actions/runs/35259047666, https://github.com/zeldadil/password-manager/actions/runs/35261467319

Re-measurement transcript: tests/evidence/t_d20787de (retro-verification bundle of card t_d20787de — cited, not this card's own evidence).
