QA-VERDICT: pass — `t_d20787de` retro-verification sweep, recorded by the `qa` profile (2026-09-19).

Scope: give the five cards that were passing on a non-QA verdict record (`t_338f47fd` §3 author rule) a real QA record, decide the deferral chain on its merits, and re-measure the audit without reopening cards or touching the gate. Every verdict below is explicitly a **retro-verification carried out after the cards were closed — not a verdict recorded at completion time**, and every claim in it was re-measured (CI API reads, fresh refs, local scanner runs), never copied from the card.

## Outcome per card

| card | retro-verdict | cleared | still red, and why |
|---|---|---|---|
| `t_e348e0b7` | pass | R1, R4 | — |
| `t_28951254` | pass-with-conditions | R1, R4 | AC5's CI run id does not exist (404); branch allowlist stale — recorded, not repairable here |
| `t_2162d273` | pass | R1 | — |
| `t_527d4720` | pass | R1, R4 | R2: immutable `architect` run-metadata token; not repairable without editing history |
| `t_c3cb6842` | pass-with-conditions | R1, R4, and R8 on `t_80fc0326` | the loose verdict path still parses a cited file name as a token (the residual half of its own AC1/AC3) |
| `t_80fc0326` | **no verdict recorded** (human-closed with a visible exception) | R8 (via `t_c3cb6842`) | R2 ×3: immutable qa-authored records; leaving them red is the honest outcome |

## Audit before → after (same clone, same gate revision `85dbc127…`)

- before: `enforced 24 · pass 16 · FAIL 8` — `t_7918f010`, `t_527d4720`, `t_80fc0326`, `t_e348e0b7`, `t_28951254`, `t_b51a1ff3`, `t_c3cb6842`, `t_2162d273`
- after: `enforced 24 · pass 20 · FAIL 4` — `t_7918f010` (out of scope, backend), `t_527d4720` (R2 only), `t_80fc0326` (R2 ×3 only), `t_b51a1ff3` (out of scope, architect)
- cleared: `t_e348e0b7`, `t_28951254`, `t_2162d273`, `t_c3cb6842`; `t_527d4720` lost R1/R4 but keeps R2; `t_80fc0326` lost R8 (R2 ×3 remain). The five in-scope cards were cleared or explicitly refused, never re-opened: all six stay `done` with `completed_at` unchanged.

## Findings carried forward (not fixed here, by scope)

1. Loose-path verdict false positive (the residual half of `t_c3cb6842`) — one regex + a regression case on the gate lane.
2. PR #31's branch carries a `QA_SIGN_OFF_GATE.md` whose §10 item 1 reverts the closure note `t_527d4720` recorded.
3. The `t_28951254` branch's allowlist is stale after a force-push rewrote the commits it suppressed; prefer deleting/re-cutting that branch.
4. `t_2162d273`'s decided theme writer is not present on any ref while FE-001d is already `done` — FE lane.

Deliverable: branch `qa/t_d20787de-retro-verify` (pushed; base `qa/t_338f47fd-marker-author`), bundle at tests/evidence/t_d20787de. No board copy is committed — the privacy decision recorded on `t_527d4720` keeps board rows on the host; only the copies' sha256 values are recorded.

Evidence: tests/evidence/t_d20787de/README.md, tests/evidence/t_d20787de/audit-t0.txt, tests/evidence/t_d20787de/audit-t1.txt, tests/evidence/t_d20787de/audit-t2.txt, tests/evidence/t_d20787de/audit-diff.txt, tests/evidence/t_d20787de/comment-readback-diff.txt
