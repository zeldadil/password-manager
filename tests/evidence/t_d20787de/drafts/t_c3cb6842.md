QA-VERDICT: pass-with-conditions — RETRO-VERIFICATION recorded by the `qa` profile (2026-09-19, from card `t_d20787de`).

This card is `qa`-assigned and was closed without any QA verdict of its own, which is why the linked-child fallback made it the deferral target of `t_80fc0326` (`R8_DEFERRAL_TARGET_INVALID`). This verdict was **not** recorded at completion time: it re-checks the three deliverables today, and it is honest about the one that does not hold.

## Deliverables, re-measured today

1. **The regex change is reviewed and merged — HOLDS.** PR #24 (`fix/verdict-marker-regex`) merged 2026-09-18T11:06Z as `f74fb094edc6d02807cd8c7ad6b9fd1d5c8f2e7f` with all 10 checks green (`install-lockfile, lint-typecheck, unit, integration, e2e, dependency-audit, secret-scan, sast, build`, `Semgrep OSS`). `origin/master:scripts/qa/signoff-gate.mjs` (blob `4c856bd3b6f2968773bf8d9fc6fd86410b151bc1`, sha256 `44e15f95…`) carries `const VERDICT_MARKER_RE = /(?:^|[\s(])qa[\s_-]*verdict\s*:\s*([a-z][a-z-]*)/i;` — the colon-only separator plus the leading boundary.
2. **Re-installed in all 7 profiles with a verified hash — HOLDS today, with forward drift.** All 7 installed copies (`architect, backend, browser, docs, frontend, product, qa`) are byte-identical to each other: sha256 `85dbc127b6262eb91133ddaf6c70b96cfdb9407bd26784717558f8452eafd814`. That revision is **not** master's `44e15f95…`: it is the reviewed-but-unmerged `t_338f47fd` revision (PR #29 + PR #31), i.e. the profiles run a later, stricter gate than master ships. The installer/verifier scripts (`scripts/qa/hooks/*`) are still not on master, so "re-install + verify hash-match" cannot be re-run from a master checkout at all.
3. **Board audit re-run — NOT MET as written.** Re-run today over the live board (copy): the audit still reports one false positive of exactly the class this card was opened for, through the **loose** verdict path. `VERDICT_LOOSE_RE` still accepts `-` and `.` as separators, so a qa comment (comment 49 on `t_80fc0326`) that cites the rotation evidence file name inside a code span — a name that begins with the marker string and ends in `-ROTATION.md` — is still parsed as an invalid token and reported as `R2_QA_VERDICT_INVALID: verdict "ROTATION"`. The marker path is fixed; the sibling path is not. Reproduced today: the gate regex applied to comment 49's body, the matched span and the resulting audit row are in `loose-path-repro.txt` and `audit-t1.txt`.

## Conditions / follow-up

- **Gate lane follow-up:** give `VERDICT_LOOSE_RE` the same treatment as the marker regex (colon-only separator, or a code-span/fence exclusion) so a cited file name can never be read as a verdict token; plus a regression case in `scripts/qa/signoff-gate.selftest.mjs` (a cited `…-ROTATION.md` must not produce a token, while a genuine `verdict: pass` still must). This is the residual half of AC1/AC3 and is reported again on `t_80fc0326`, whose `R2` will stay red until it lands.
- **Merge lane follow-up:** this branch's `QA_SIGN_OFF_GATE.md` §10 item 1 duplicates the pre-decision text that `t_527d4720` closed; the pending PR #31 lane must not lose that closure note.

The reproduction of the loose-path parse (the gate regex applied to comment 49's body, the matched span, and the resulting audit row) is captured in this sweep's own output: the files loose-path-repro.txt and audit-t1.txt in the t_d20787de evidence bundle (tests/evidence/t_d20787de, branch qa/t_d20787de-retro-verify).

Evidence: scripts/qa/signoff-gate.mjs, https://github.com/zeldadil/password-manager/pull/24

Re-measurement transcripts: tests/evidence/t_d20787de (retro-verification bundle of card t_d20787de — cited, not this card's own evidence).
