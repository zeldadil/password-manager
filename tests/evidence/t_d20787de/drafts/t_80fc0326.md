Retro-verification note (qa, 2026-09-19, from card `t_d20787de`) — **no QA verdict is recorded here, deliberately.**

`t_d20787de` re-checked this card so the board audit reflects what was actually verified. Two statements, both measured today rather than taken from the card's own summary.

**1. QA does not record a terminal verdict on this card.** The last point-in-time QA records are the changes-requested review (comments 62 and 63); the card was then closed by human decision with the exception marker in comment 80, which the audit already surfaces as `X1_EXCEPTION`. Recording a "pass" now would rewrite that history, and QA will not reopen the card or retouch the gate to make the audit green.

**2. Current state, re-measured (2026-09-19, fresh refs, 40 remote branch tips).** 13 tips still carry a token-shaped string. The three distinct values involved are **all revoked**: the originally leaked value (`getMe` → 401; 11 tips, every one of them the `t_e348e0b7` raw reproduction file), a synthetic placeholder (401), and the value propagated by `t_28951254` (401). No live credential exists on any of the 40 tips, and a full-history truffleHog run over the clone reports `verified_secrets: 0, unverified_secrets: 0`. The residual is therefore exactly what the human ruling describes: dead-credential hygiene, not live exposure.

**Gate mechanics, recorded so the red rows are not mysterious:**

- The `R8_DEFERRAL_TARGET_INVALID` row this card carried is resolved: its deferral target `t_c3cb6842` now carries a retro-verified QA verdict together with evidence, so the linked-child chain is satisfied at its root — a `qa` card that was `done` without a verdict.
- The three `R2_QA_VERDICT_INVALID` rows remain, and should remain. They are immutable qa-authored records (comments 49, 62, 63) whose text carries off-vocabulary tokens; one of them is also the loose-path false positive tracked on `t_c3cb6842`. Only editing history would clear them, and QA will not do that.

Transcripts: tests/evidence/t_d20787de (retro-verification bundle of card t_d20787de).
