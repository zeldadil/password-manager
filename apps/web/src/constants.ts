const AUTO_LOCK_TIMEOUT_MS = 15 * 60 * 1000 // 15 min — must match server config
const WARNING_THRESHOLD_MS = 60 * 1000 // show banner when 1 min remains

export { AUTO_LOCK_TIMEOUT_MS, WARNING_THRESHOLD_MS }
