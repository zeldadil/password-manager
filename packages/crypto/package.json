{
  "name": "@password-manager/crypto",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Crypto primitives — Argon2id KDF, AES-256-GCM AEAD, key wrap/unwalk (SEC-001)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --noEmit && echo 'typecheck ok'",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "argon2": "file:../../../../../tmp/argon2-0.45.1.tgz"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^5.0.0"
  }
}
